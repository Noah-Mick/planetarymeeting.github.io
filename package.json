{
  "name": "planetarymeeting",
  "version": "1.0.0",
  "description": "Planetary Meeting — art portfolio for Livia Burkhardt. Static site, no build step required to view.",
  "private": true,
  "scripts": {
    "serve": "node scripts/serve.js",
    "build": "node scripts/build-gallery.js",
    "sync": "node scripts/sync-dropbox.js",
    "auth": "node scripts/dropbox-auth.js"
  },
  "devDependencies": {
    "sharp": "^0.33.5"
  }
}
